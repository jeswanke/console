/** Log line prefixes for globalSetup / cluster / GitOps (grep-friendly). */
export const LOG_GLOBAL_SETUP = '[Global setup]';
export const LOG_CLUSTER_PREP = '[Cluster prep]';
export const LOG_GITOPS_PREP = '[GitOps prep]';
export const LOG_ANSIBLE_PREP = '[Ansible prep]';
